package com.example.b1void.ui.camera

import androidx.compose.runtime.Composable
import com.example.b1void.feature.camera.presentation.viewmodel.CameraViewModel

// Stubbed composable to fix build
@Composable
fun CameraScreen(viewModel: CameraViewModel) {
    // TODO: REWRITE REQUIRED. Legacy code commented out to fix build.
}