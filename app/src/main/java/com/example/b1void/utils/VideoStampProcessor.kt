package com.example.b1void.utils

import android.content.Context
import java.io.File

// Stubbed class to fix build
class VideoStampProcessor(private val context: Context) {
    suspend fun addTimestampToVideo(input: File, output: File): Boolean {
        return false
    }
}